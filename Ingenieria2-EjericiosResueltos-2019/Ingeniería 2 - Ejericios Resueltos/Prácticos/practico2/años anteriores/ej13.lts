P = ( a -> ( b -> STOP
           | tao -> c -> STOP)
    ).

Q = ( a -> ( b -> STOP
           | tao -> c -> STOP)
    | a -> c -> STOP
    ).
||TP = P\{tao}. // ik it's like cheating
||TQ = Q\{tao}. // deal with it.