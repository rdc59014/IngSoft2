const F = 10
range B = 0..F

property ELEVADER = ELEVADER[0],
ELEVADER[i:B] = (when(i < F) enter -> ELEVADER[i+ 1]
                |when(i > 0) exit -> ELEVADER[i - 1]).

ELEVATOR = (enter -> ELEVATOR
           |exit -> ELEVATOR).

||RES = (ELEVADER || ELEVATOR).